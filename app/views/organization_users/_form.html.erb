<%= simple_form_for(@organization_user) do |f| %>
  <% if @organization_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization_user.errors.count, "error") %> prohibited this organization_user from being saved:</h2>

      <ul>
      <% @organization_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :org_id, :as => :hidden, :input_html => { :value => @org_id } %>
  <%= f.input :user_id, :as => :hidden, :input_html => { :value => current_user.id } %>

  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
